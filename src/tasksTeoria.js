export const teoriaTasks = [
  {
    id: "teoria-1",
    module: "Основы",
    stack: "JavaScript",
    card: "Code structure",
    theory: `## Структура кода и механизмы ASI

В JavaScript инструкции (statements) обычно разделяются точкой с запятой (\`;\`). Но язык спроектирован так, чтобы быть "дружелюбным" к новичкам, поэтому в нем существует механизм **ASI**.

### 1. Что такое ASI (Automatic Semicolon Insertion)?
Это алгоритм, встроенный в парсер JavaScript, который автоматически вставляет виртуальные точки с запятой в исходный код во время синтаксического анализа. 

**Парсер вставляет \`;\`, если:**

1. Инструкции разделены переводом строки, и их нельзя объединить в одну валидную инструкцию.
2. Встречается закрывающая фигурная скобка \`}\`.
3. Достигнут конец потока исходного кода.

### 2. "Ловушка" Restricted Productions
Существуют инструкции, в которых перевод строки **запрещен** спецификацией сразу после ключевого слова. Если он там есть — ASI сработает мгновенно и "оборвет" вашу логику.

К ним относятся:
- \`return\`
- \`throw\`
- \`break\`
- \`continue\`
- \`yield\` (в генераторах)

**Пример ошибки:**
\`\`\`js
return 
  { status: 200 };
// Парсер видит: return; { status: 200 };
// Результат: undefined
\`\`\`

### 3. Ситуации, когда ASI НЕ сработает (Danger Zone)
ASI **не вставит** точку с запятой, если следующая строка начинается с символов, которые могут продолжить текущее выражение. Самые опасные из них:
- Круглые скобки \`(\` (интерпретируется как вызов функции)
- Квадратные скобки \`[\` (интерпретируется как доступ к свойству или деструктуризация)
- Косая черта \`/\` (может быть воспринята как начало регулярного выражения в составе операции деления)

**Пример катастрофы:**
\`\`\`js
let user = "Alex"
["Admin"].forEach(console.log)
// Парсер видит: let user = "Alex"["Admin"].forEach(...)
// Итог: TypeError (нельзя прочитать property 'forEach' у undefined)
\`\`\`

### 4. Современный стандарт разработки
Хотя современные инструменты (линтеры) позволяют писать код без точек с запятой, в среде **Middle/Senior** разработчиков BigTech-компаний принято:
1. **Явное указание**: Ставить \`;\` всегда. Это исключает двусмысленность для парсера и других разработчиков.
2. **Defensive Semicolon**: Если вы используете стиль без \`;\`, обязательно ставьте точку с запятой в начале строки, если она начинается с \`[\` или \`(\`.`,

    questions: [
      {
        id: 1,
        question: "Что такое ASI в JavaScript?",
        expected:
          "ASI (Automatic Semicolon Insertion) — это механизм автоматической вставки точки с запятой парсером на этапе анализа кода, если инструкции разделены переносом строки и не могут быть объединены.",
        keywords: ["автоматическая", "вставка", "парсер", "точка с запятой"],
      },
      {
        id: 2,
        question: "К чему приведет перенос строки сразу после ключевого слова 'return'?",
        expected:
          "К автоматической вставке точки с запятой (ASI), из-за чего функция вернет undefined, а код на следующей строке не будет выполнен как часть возвращаемого значения.",
        keywords: ["undefined", "ASI", "перенос", "оборвет"],
      },
      {
        id: 3,
        question: "Почему код, начинающийся с квадратных скобок '[' на новой строке, опасен без точек с запятой?",
        expected:
          "Потому что парсер не вставит точку с запятой и попытается интерпретировать квадратные скобки как обращение к свойству переменной с предыдущей строки.",
        keywords: ["свойство", "обращение", "индекс", "не вставит"],
      },
      {
        id: 4,
        question: "В каких случаях ASI не срабатывает, несмотря на перенос строки?",
        expected:
          "ASI не срабатывает, если текущая инструкция может быть логически продолжена на следующей строке (например, если строка начинается с '(', '[', или бинарного оператора).",
        keywords: ["продолжение", "логически", "скобки", "оператор"],
      },
      {
        id: 5,
        question: "Что такое 'Restricted Productions' в контексте Code Structure?",
        expected:
          "Это набор инструкций (return, throw, break, continue), после которых запрещен перевод строки, так как это вызывает немедленную вставку точки с запятой.",
        keywords: ["return", "запрещен", "перенос", "инструкции"],
      },
      {
        id: 6,
        question: "Как использование ESLint или Prettier помогает в контексте структуры кода?",
        expected:
          "Они автоматизируют форматирование, расставляют точки с запятой согласно конфигу и подсвечивают опасные места, где ASI может сработать непредсказуемо.",
        keywords: ["автоматизация", "форматирование", "подсветка", "конфиг"],
      },
      {
        id: 7,
        question: "Какой символ, кроме переноса строки, может спровоцировать ASI?",
        expected:
          "Закрывающая фигурная скобка '}', так как она сигнализирует о завершении блока кода, что позволяет парсеру считать предыдущую инструкцию оконченной.",
        keywords: ["фигурная скобка", "закрытие", "блок"],
      },
    ],

    cooldownHours: 20,
    xpReward: 30,
    aiPointsReward: 10,
  },

  {
    id: "teoria-2",
    module: "Основы",
    stack: "JavaScript",
    card: "Use strict",
    theory: `## Строгий режим: "use strict"

Директива \`"use strict"\` — это специальная инструкция, которая переводит код в современный режим работы, изменяя поведение некоторых функций языка и добавляя более строгие проверки на ошибки.

### 1. Механика активации
- **Глобально**: Размещается в самой первой строке скрипта.
- **Локально**: Размещается в начале тела функции.
- **Автоматически**: Всегда включен внутри модулей (\`ES Modules\`) и классов (\`class\`).
- **Необратимость**: Строгий режим невозможно отключить в середине выполнения скрипта или внутри вложенных контекстов.

### 2. Ключевые изменения семантики
Строгий режим предотвращает "молчаливые" ошибки, превращая их в исключения (\`Throw Error\`):

1. **Глобальные переменные**: Запрещает неявное создание переменных. Присвоение значения необъявленному идентификатору вызывает \`ReferenceError\`.
2. **Контекст this**: Значение \`this\` в функциях, вызванных не как методы (без контекста), будет равно \`undefined\`, а не глобальному объекту \`window/global\`. Это защищает от случайного загрязнения глобальной области видимости.
3. **Безопасность свойств**: Запрещает запись в свойства, доступные только для чтения (\`read-only\`), и удаление (\`delete\`) невыдаляемых свойств объектов или переменных.
4. **Дублирование параметров**: Вызывает синтаксическую ошибку при наличии одинаковых имен аргументов в функции (например, \`function sum(a, a, b)\`).

### 3. Запрещенный синтаксис
Для оптимизации и чистоты кода в строгом режиме удалены устаревшие конструкции:
- **Восьмеричная система**: Запрещен синтаксис чисел через ноль (\`0123\`).
- **Оператор \`with\`**: Полностью запрещен, так как он делает невозможным статический анализ области видимости.
- **Будущие зарезервированные слова**: Нельзя использовать в качестве имен переменных такие слова, как \`implements\`, \`interface\`, \`let\`, \`package\`, \`private\`, \`protected\`, \`public\`, \`static\`, \`yield\`.

### 4. Особенности eval и arguments
- **eval**: Имеет собственную область видимости. Переменные, созданные внутри \`eval\`, не попадают в лексическое окружение вызывающего кода.
- **arguments**: Изменения значений аргументов функции не синхронизируются с объектом \`arguments\`, что делает код более предсказуемым для оптимизаторов JIT.`,

    questions: [
      {
        id: 1,
        question: "Где именно должна располагаться директива 'use strict', чтобы она работала?",
        expected:
          "Директива должна находиться в самом начале скрипта или функции. До неё могут быть только комментарии, иначе она не будет распознана как инструкция перехода в строгий режим.",
        keywords: ["начало", "первая строка", "скрипт", "функция"],
      },
      {
        id: 2,
        question: "Какое значение примет 'this' в обычной функции, вызванной без контекста, в строгом режиме?",
        expected:
          "В строгом режиме значение 'this' будет равно undefined, в то время как в обычном режиме оно ссылается на глобальный объект (window или global).",
        keywords: ["undefined", "неопределено", "window", "глобальный"],
      },
      {
        id: 3,
        question: "Что произойдет при попытке присвоить значение переменной, которая не была объявлена через let/const/var?",
        expected:
          "Движок JavaScript выбросит исключение ReferenceError, так как в строгом режиме запрещено автоматическое создание глобальных переменных.",
        keywords: ["ReferenceError", "ошибка", "исключение", "объявлена"],
      },
      {
        id: 4,
        question: "Какие современные конструкции JavaScript включают строгий режим автоматически?",
        expected:
          "Строгий режим всегда активен внутри классов (class) и во всех модулях (ES Modules) по умолчанию.",
        keywords: ["классы", "модули", "class", "modules", "автоматически"],
      },
      {
        id: 5,
        question: "Почему в строгом режиме запрещен оператор 'with'?",
        expected:
          "Оператор 'with' делает код неоднозначным и мешает статическому анализу, так как невозможно заранее определить, ссылается ли имя на свойство объекта или на переменную в области видимости.",
        keywords: ["неоднозначность", "анализ", "область видимости", "with"],
      },
      {
        id: 6,
        question: "Как меняется поведение 'eval' при активации 'use strict'?",
        expected:
          "В строгом режиме 'eval' создает свою собственную область видимости, поэтому переменные и функции, объявленные внутри него, не будут доступны снаружи.",
        keywords: ["область видимости", "собственная", "доступны", "внутри"],
      },
      {
        id: 7,
        question: "Можно ли отключить строгий режим для части кода внутри скрипта, где он включен глобально?",
        expected:
          "Нет, включение строгого режима является необратимым для всего контекста исполнения, в котором он был объявлен.",
        keywords: ["нет", "нельзя", "необратимо", "невозможно"],
      },
    ],

    cooldownHours: 20,
    xpReward: 30,
    aiPointsReward: 10,
  }
];

export const getTeoriaTaskByCard = (card, module, stack) => {
  return teoriaTasks.find(
    (t) => t.card.toLowerCase() === card?.toLowerCase() && 
           t.module === module && 
           t.stack === stack,
  );
};

export const getAllTeoriaCards = () => {
  return teoriaTasks.map((t) => ({
    card: t.card,
    module: t.module,
    stack: t.stack,
    questionsCount: t.questions.length,
    cooldownHours: t.cooldownHours,
    xpReward: t.xpReward,
    aiPointsReward: t.aiPointsReward,
  }));
};
